import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic_alcool-ou-gasolina',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
